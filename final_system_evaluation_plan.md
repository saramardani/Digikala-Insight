# برنامهٔ ارزیابی سیستم نهایی

## هدف

برای بخش‌های «جست‌وجو و کشف محصول»، «پرسش‌وپاسخ مبتنی بر نظرات»،
«مقایسهٔ محصولات» و «تحلیل برای مدیران فروشگاه»، یک ارزیابی قابل‌دفاع
انجام شود. هرجا پاسخ مرجع وجود دارد، معیار عددی محاسبه می‌شود؛ هرجا کیفیت
پاسخ طبیعی، ارتباط و کفایت شواهد مهم است، ارزیابی انسانی انجام می‌شود.

ارزیابی `recommendation_status` جداگانه با **Macro-F1** روی test set گزارش
می‌شود و با تحلیل کیفی خطاها کامل خواهد شد.

## وضعیت فعلی

- evaluator موجود ۱۱ case دارد: ۹ مقایسه و ۲ resolver.
- grounding خودکار وجود دارد، اما فرم human annotation هنوز تکمیل نشده است.
- qrelهای retrieval اولیه‌اند و باید با داوری انسانی تأیید شوند.
- روش production فعلی BM25 است. Dense، Hybrid و Reranker به‌دلیل محدودیت
  RAM در benchmark فعلی unavailable هستند؛ نباید کیفیت آن‌ها ادعا شود.
- تحلیل مدیریتی هنوز باید به خروجی‌ها و caseهای مشخص تبدیل شود.

## اصل نسخه‌بندی

مجموعهٔ فعلی `final_v1` نباید تغییر کند. مجموعهٔ جدید با نامی مانند
`final_v2` یا `final_evaluation_v2` ایجاد می‌شود و شامل موارد زیر است:

- case set ثابت و نسخه‌دار؛
- پاسخ‌ها و evidenceهای تولیدشده؛
- annotationهای انسانی؛
- manifest شامل dataset revision، تنظیمات retrieval، مدل generation و seed؛
- گزارش نهایی Markdown و JSON/CSV/Parquet.

## حجم پیشنهادی ارزیابی

۲۰ محصول به‌شکل **stratified** انتخاب شوند؛ فقط انتخاب محصولات پُرنظر کافی
نیست. نمونه باید شامل دسته‌های گوناگون و حالت‌های زیر باشد:

- محصول پُرنظر با رضایت بالا؛
- محصول پُرنظر با رضایت پایین؛
- محصول کم‌نظر یا بدون evidence؛
- محصول با metadata متناقض؛
- نام مبهم یا variantهای مشابه.

| بخش | موضوع ارزیابی | تعداد پیشنهادی |
|---|---|---:|
| جست‌وجو و کشف محصول | درستی محصول، ابهام و variant | ۲۰ تا ۳۰ پرسش |
| QA مبتنی بر نظر | پاسخ، citation و grounding | ۲۰ تا ۳۰ پاسخ |
| مقایسهٔ محصول | برنده، نامطمئن، تناقض و چندمحصولی | ۳۰ تا ۴۰ پاسخ |
| تحلیل مدیریتی | درستی و اقدام‌پذیری insight | ۲۰ تا ۳۰ پاسخ |
| prediction recommendation_status | Macro-F1 و نمونه‌های خطا | معیار عددی + ۱۰ تا ۲۰ نمونه |

هدف، حدود ۱۰۰ تا ۱۵۰ پاسخ است. در صورت توان، می‌توان آن را تا ۲۰۰ پاسخ
گسترش داد.

## ۱. جست‌وجو و کشف محصول

برای هر query یک پاسخ مرجع انسانی ثبت شود: `product_id` درست، یا وضعیت
`ambiguous` یا `not_found`.

سناریوهای لازم:

- نام دقیق محصول؛
- نام ناقص یا دارای غلط املایی؛
- برند و مدل؛
- variantهای مشابه مانند ظرفیت، رنگ، 4G/5G و Pro/Max؛
- نام واقعاً مبهم؛
- مدل یا variant نامعتبر.

معیارها:

- Exact match accuracy؛
- accuracy وضعیت ambiguous/not_found؛
- Variant-protection accuracy؛
- Human relevance@K، در صورت نمایش چند candidate.

## ۲. پرسش‌وپاسخ مبتنی بر نظرات

سوال‌های QA موجود به case set جدید اضافه شوند. برای هر پاسخ موارد زیر بررسی
شود:

- پاسخ به سؤال مرتبط است؛
- review_idهای citation شده واقعی‌اند؛
- review به محصول درست تعلق دارد؛
- review واقعاً ادعای متن را پشتیبانی می‌کند؛
- آمار کلی از تمام reviewهای واجد شرایط آمده، نه Top-K evidence؛
- در نبود evidence کافی، سیستم abstain یا inconclusive می‌دهد.

## ۳. مقایسهٔ محصولات

برای هر case، پیش از تولید LLM، پاسخ مرجع deterministic ثبت شود:

- محصول‌ها و معیارها؛
- برندهٔ هر معیار یا `inconclusive`؛
- reason code و practical threshold؛
- مقدارها، numerator/denominator و support؛
- وزن‌های اعلام‌شده توسط کاربر، در صورت وجود.

سناریوهای لازم:

- برندهٔ روشن در دادهٔ مستقیم؛
- برندهٔ روشن در رضایت full-population؛
- نزدیک بودن نتایج و inconclusive؛
- metadata متناقض؛
- evidence کم یا صفر؛
- بازخوردهای مثبت و منفی مخلوط؛
- مقایسهٔ بیش از دو محصول؛
- برندهٔ کلی با اولویت‌های صریح کاربر.

LLM فقط توضیح می‌دهد. برنده و آمار باید از محاسبهٔ deterministic بیایند و
LLM نباید آن‌ها را تغییر دهد.

## ۴. تحلیل برای مدیران فروشگاه

ابتدا خروجی‌های مدیریتی مشخص و قابل‌محاسبه ساخته شوند. حداقل چهار سناریو:

1. محصولات پُرنظر با درصد توصیهٔ پایین؛
2. برندها یا دسته‌ها با کمترین رضایت؛
3. محصولات با شکاف زیاد میان Rate محصول و رضایت reviewها؛
4. شکایت‌ها یا نکات مثبت پرتکرار در یک دسته.

هر رتبه‌بندی و مقدار پایه باید با Polars/Python و دادهٔ کامل محاسبه شود.
داوری انسانی فقط کیفیت بیان insight را می‌سنجد، نه جایگزین محاسبهٔ مرجع.

## ۵. ارزیابی انسانی

برای هر پاسخ، این ستون‌ها در CSV annotation ثبت شوند:

- `annotator_id`؛
- relevance از ۱ تا ۵؛
- clarity از ۱ تا ۵؛
- source separation از ۱ تا ۵؛
- recommendation usefulness از ۱ تا ۵؛
- uncertainty appropriateness از ۱ تا ۵؛
- citation usefulness از ۱ تا ۵؛
- semantic citation support از ۱ تا ۵؛
- درست بودن محصول/برنده/abstain؛
- comment و failure category.

بهتر است دو ارزیاب، دست‌کم بخشی از نمونه‌ها را مستقل ارزیابی کنند. میانگین
امتیازها و توافق بین ارزیاب‌ها جداگانه گزارش شود.

## ۶. Retrieval

qrelهای فعلی seed candidate هستند و ground truth نهایی محسوب نمی‌شوند. برای
اعتبار عددی retrieval:

- حداقل ۲۰ query representative انتخاب شود؛
- برای هر query، Top-10 evidence توسط انسان relevant/not-relevant برچسب بخورد؛
- `Recall@K`، `Precision@K`، `MRR` و `NDCG@K` با qrelهای تأییدشده گزارش شوند؛
- retrieval quality از generation quality جدا گزارش شود.

## ۷. پیش‌بینی recommendation_status

معیار اصلی:

- Macro-F1 روی test set ثابت.

همراه آن گزارش شود:

- confusion matrix؛
- F1 هر کلاس، به‌ویژه `not_recommended`؛
- اندازه و تعریف test set؛
- ۱۰ تا ۲۰ نمونهٔ خطا با تحلیل علت.

## ۸. ترتیب اجرا

1. سوال‌های QA فعلی را وارد case set نسخهٔ ۲ کنید.
2. ۲۰ محصول stratified انتخاب کنید.
3. caseهای مرجع search، comparison و manager-analysis را بنویسید.
4. qrelهای retrieval را انسانی تأیید کنید.
5. پاسخ‌های واقعی سیستم را با تنظیمات frozen تولید کنید.
6. annotation انسانی را تکمیل کنید.
7. Macro-F1 prediction و تحلیل خطا را به گزارش اضافه کنید.
8. گزارش نهایی را با محدودیت‌ها، هزینه، latency، مدل‌ها و موارد
   `inconclusive` منتشر کنید.

## نکات گزارش نهایی

- denominator تمام معیارها و تعداد پاسخ‌های حذف‌شده/ناموفق مشخص باشد.
- Top-K evidence هرگز به‌جای آمار کل محصول استفاده نشود.
- نتیجهٔ نامطمئن خطا نیست؛ اگر evidence یا داده ناکافی/متناقض است، باید
  `inconclusive` گزارش شود.
- ارزیابی انسانی و retrieval از معیارهای grounding و Macro-F1 جدا گزارش شوند.
- محدودیت Dense/Hybrid/Reranker در سخت‌افزار فعلی صریحاً ذکر شود.
